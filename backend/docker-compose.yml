services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: wordquiz
      POSTGRES_USER: wordquiz
      POSTGRES_PASSWORD: wordquiz
    ports:
      - "5432:5432"
    volumes:
      - wordquiz_pgdata:/var/lib/postgresql/data

volumes:
  wordquiz_pgdata:
